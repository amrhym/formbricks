-- Superset analytics views
-- These views are applied separately by the startup script (next-start.sh)
-- after all schema migrations complete, ensuring all referenced tables exist.
-- This migration is intentionally empty to prevent blocking other migrations.
SELECT 1;
